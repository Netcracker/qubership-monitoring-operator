{{- if and .Values.vmAuth.install .Values.ingress.install }}
{{- if .Values.ingress.vmAuth }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ default (printf "%s-vmauth" (include "monitoring-pack-one.fullname" .)) .Values.vmAuth.name }}-ingress
  namespace: {{ include "monitoring-pack-one.namespace" . }}
  labels:
    name: vmauth-ingress
    app.kubernetes.io/name: vmauth
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: monitoring
    app.kubernetes.io/managed-by: monitoring-operator
    {{- $labelCtx := dict "commonLabels" .Values.commonLabels "componentLabels" (.Values.ingress.vmAuth.labels | default dict) }}
    {{- include "monitoring-pack-one.metadata.labels" $labelCtx | nindent 4 }}
  annotations:
    {{- $annotationCtx := dict "commonAnnotations" .Values.commonAnnotations "componentAnnotations" (.Values.ingress.vmAuth.annotations | default dict) }}
    {{- include "monitoring-pack-one.metadata.annotations" $annotationCtx | nindent 4 }}
spec:
  {{- if .Values.ingress.vmAuth.ingressClassName }}
  ingressClassName: {{ .Values.ingress.vmAuth.ingressClassName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.vmAuth.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vmauth-{{ default (printf "%s-vmauth" (include "monitoring-pack-one.fullname" .)) .Values.vmAuth.name }}
                port:
                  number: {{ .Values.ingress.vmAuth.servicePort | default 8427 }}
  {{- if .Values.ingress.vmAuth.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.ingress.vmAuth.host }}
      secretName: {{ .Values.ingress.vmAuth.tlsSecretName }}
  {{- end }}
{{- end }}
{{- end }}

{{- if and .Values.vmAgent.install .Values.ingress.install }}
{{- if .Values.ingress.vmAgent }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ default (printf "%s-vmagent" (include "monitoring-pack-one.fullname" .)) .Values.vmAgent.name }}-ingress
  namespace: {{ include "monitoring-pack-one.namespace" . }}
  labels:
    name: vmagent-ingress
    app.kubernetes.io/name: vmagent
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: monitoring
    app.kubernetes.io/managed-by: monitoring-operator
    {{- $labelCtx := dict "commonLabels" .Values.commonLabels "componentLabels" (.Values.ingress.vmAgent.labels | default dict) }}
    {{- include "monitoring-pack-one.metadata.labels" $labelCtx | nindent 4 }}
  annotations:
    {{- $annotationCtx := dict "commonAnnotations" .Values.commonAnnotations "componentAnnotations" (.Values.ingress.vmAgent.annotations | default dict) }}
    {{- include "monitoring-pack-one.metadata.annotations" $annotationCtx | nindent 4 }}
spec:
  {{- if .Values.ingress.vmAgent.ingressClassName }}
  ingressClassName: {{ .Values.ingress.vmAgent.ingressClassName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.vmAgent.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                {{- if .Values.vmAuth.install }}
                # Route through VMAuth for authentication
                # VictoriaMetrics Operator creates service as vmauth-{name}, not {name}-vmauth
                name: vmauth-{{ default (printf "%s-vmauth" (include "monitoring-pack-one.fullname" .)) .Values.vmAuth.name }}
                {{- else }}
                name: {{ default (printf "%s-vmagent" (include "monitoring-pack-one.fullname" .)) .Values.vmAgent.name }}-vmagent
                {{- end }}
                port:
                  number: {{ ternary (.Values.ingress.vmAuth.servicePort | default 8427) (.Values.ingress.vmAgent.servicePort | default 8429) .Values.vmAuth.install }}
  {{- if .Values.ingress.vmAgent.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.ingress.vmAgent.host }}
      secretName: {{ .Values.ingress.vmAgent.tlsSecretName }}
  {{- end }}
{{- end }}
{{- end }}

{{- if .Values.ingress.install }}
{{- if .Values.ingress.vmSingle }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.vmSingle.name }}-ingress
  namespace: {{ .Values.vmSingle.namespace }}
  labels:
    name: vmsingle-ingress
    app.kubernetes.io/name: vmsingle
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: monitoring
    app.kubernetes.io/managed-by: monitoring-operator
    {{- $labelCtx := dict "commonLabels" .Values.commonLabels "componentLabels" (.Values.ingress.vmSingle.labels | default dict) }}
    {{- include "monitoring-pack-one.metadata.labels" $labelCtx | nindent 4 }}
  annotations:
    {{- $annotationCtx := dict "commonAnnotations" .Values.commonAnnotations "componentAnnotations" (.Values.ingress.vmSingle.annotations | default dict) }}
    {{- include "monitoring-pack-one.metadata.annotations" $annotationCtx | nindent 4 }}
spec:
  {{- if .Values.ingress.vmSingle.ingressClassName }}
  ingressClassName: {{ .Values.ingress.vmSingle.ingressClassName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.vmSingle.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.vmSingle.serviceName | default (printf "%s-k8s" .Values.vmSingle.name) }}
                port:
                  number: {{ .Values.ingress.vmSingle.servicePort | default 8429 }}
  {{- if .Values.ingress.vmSingle.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.ingress.vmSingle.host }}
      secretName: {{ .Values.ingress.vmSingle.tlsSecretName }}
  {{- end }}
{{- end }}
{{- end }}
{{- if and .Values.vmAlert.install .Values.vmAuth.install .Values.ingress.install }}
{{- if .Values.ingress.vmalert }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ default (printf "%s-vmalert" (include "monitoring-pack-one.fullname" .)) .Values.vmAlert.name }}-ingress
  namespace: {{ include "monitoring-pack-one.namespace" . }}
  labels:
    name: vmalert-ingress
    app.kubernetes.io/name: vmalert
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: monitoring
    app.kubernetes.io/managed-by: monitoring-operator
    {{- $labelCtx := dict "commonLabels" .Values.commonLabels "componentLabels" (.Values.ingress.vmalert.labels | default dict) }}
    {{- include "monitoring-pack-one.metadata.labels" $labelCtx | nindent 4 }}
  annotations:
    {{- $annotationCtx := dict "commonAnnotations" .Values.commonAnnotations "componentAnnotations" (.Values.ingress.vmalert.annotations | default dict) }}
    {{- include "monitoring-pack-one.metadata.annotations" $annotationCtx | nindent 4 }}
spec:
  {{- if .Values.ingress.vmalert.ingressClassName }}
  ingressClassName: {{ .Values.ingress.vmalert.ingressClassName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.vmalert.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                # Route through VMAuth for authentication
                # VictoriaMetrics Operator creates service as vmauth-{name}, not {name}-vmauth
                name: vmauth-{{ default (printf "%s-vmauth" (include "monitoring-pack-one.fullname" .)) .Values.vmAuth.name }}
                port:
                  number: {{ .Values.ingress.vmAuth.servicePort | default 8427 }}
  {{- if .Values.ingress.vmalert.tlsSecretName }}
  tls:
    - hosts:
        - {{ .Values.ingress.vmalert.host }}
      secretName: {{ .Values.ingress.vmalert.tlsSecretName }}
  {{- end }}
{{- end }}
{{- end }}
