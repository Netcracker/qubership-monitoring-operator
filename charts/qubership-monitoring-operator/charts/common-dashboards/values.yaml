# Default values for common-dashboards.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Allow to enable deploy common-dashboards
install: false

# Dashboard for Kafka Java Clients with Producer and Consumer Metrics
kafkaJavaClientsDashboard:
  install: false

backupDaemonDashboard: false
