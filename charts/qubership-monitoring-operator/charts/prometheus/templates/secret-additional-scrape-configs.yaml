apiVersion: v1
kind: Secret
metadata:
  name: additional-scrape-configs
  labels:
    {{- include "monitoring.labels" (dict "ctx" . "name" "additional-scrape-configs") | nindent 4 }}
type: Opaque
{{- if .Values.additionalScrape }}
stringData:
  prometheus-additional.yaml: |-
    {{- toYaml .Values.additionalScrape | nindent 4 }}
{{- else }}
data:
  prometheus-additional.yaml: ""
{{- end }}
