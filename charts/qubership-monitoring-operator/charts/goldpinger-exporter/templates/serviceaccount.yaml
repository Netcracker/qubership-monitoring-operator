{{- if and .Values.install .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "goldpinger.serviceAccountName" . }}
  labels:
    {{- include "common-labels.resourceLabels" (dict "ctx" . "name" (include "goldpinger.serviceAccountName" .) "component" "goldpinger-exporter") | nindent 4 }}
{{- end }}
