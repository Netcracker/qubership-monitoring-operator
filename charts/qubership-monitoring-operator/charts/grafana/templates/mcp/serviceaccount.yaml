{{- if and .Values.mcp.install .Values.mcp.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "grafana.mcp.fullname" .) .Values.mcp.serviceAccount.name }}
  labels:
    {{- include "monitoring.labels" (dict "ctx" . "name" (include "grafana.mcp.fullname" .)) | nindent 4 }}
    {{- if .Values.mcp.serviceAccount.labels }}
    {{- include "monitoring.extraLabels" (dict "ctx" . "extraLabels" .Values.mcp.serviceAccount.labels) | nindent 4 }}
    {{- end }}
  {{- if .Values.mcp.serviceAccount.annotations }}
  annotations:
    {{- toYaml .Values.mcp.serviceAccount.annotations | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.mcp.serviceAccount.automountServiceAccountToken }}
{{- end }}
