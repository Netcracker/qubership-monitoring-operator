{{- if and .Values.vmAuth.install .Values.vmAuth.extraVarsSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.vmAuth.secretName }}
  labels:
    {{- include "common-labels.resourceLabels" (dict "ctx" . "name" .Values.vmAuth.secretName "component" "victoriametrics") | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.vmAuth.extraVarsSecret }}
  {{ $key | quote | indent 6}}: {{ $val | quote }}
  {{- end}}
{{- end }}
