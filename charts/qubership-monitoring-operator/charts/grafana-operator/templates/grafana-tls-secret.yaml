{{- if and .Values.tlsEnabled (not .Values.tlsConfig.existingSecret) }}
{{- if .Values.tlsConfig.createSecret }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ default "grafana-tls-secret" .Values.tlsConfig.createSecret.secretName }}
  labels:
    name: {{ default "grafana-tls-secret" .Values.tlsConfig.createSecret.secretName }}
    app.kubernetes.io/name: {{ default "grafana-tls-secret" .Values.tlsConfig.createSecret.secretName }}
    app.kubernetes.io/instance: {{ cat (default "grafana-tls-secret" .Values.tlsConfig.createSecret.secretName) "-" .Release.Namespace | nospace | trunc 63 | trimSuffix "-" }}
    app.kubernetes.io/component: grafana
data: {}
stringData:
  {{- if .Values.tlsConfig.createSecret.ca }}
  ca.crt:
    {{- toYaml .Values.tlsConfig.createSecret.ca | nindent 4 }}
  {{- end }}
  {{- if .Values.tlsConfig.createSecret.cert }}
  tls.crt:
    {{- toYaml .Values.tlsConfig.createSecret.cert | nindent 4 }}
  {{- end }}
  {{- if .Values.tlsConfig.createSecret.key }}
  tls.key:
    {{- toYaml .Values.tlsConfig.createSecret.key | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
